{
  "name": "turtleneck",
  "version": "0.1.0",
  "private": true,
  "description": "A grounded, advisory design-critic skill for coding agents.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node scripts/build.js --check",
    "prepare": "lefthook install || true"
  },
  "devDependencies": {
    "lefthook": "^1.6.0"
  }
}
